# Consumer examples

This folder is reserved for example consumer snippets — copy-paste-ready `.github/workflows/ci.yml` files for module repos and project monorepos.

Will be populated alongside `terraform-aws-vpc` v1.0.0 (Friday's deliverable) so the first example is real, not theoretical.

For now, see the [README](../README.md) for inline consumption snippets.
